@model IEnumerable<ShopCar_pok.Models.ProductCategory>

@{
    ViewData["Title"] = "List";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>List</h1>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.ActionLink("PriductCategoryID", "List", new{ sortOrder = ViewBag.PriductCategoryID})
            </th>
            <th>
                @Html.ActionLink("Name", "List", new { sortOrder = ViewBag.Name })
            </th>
            @*
            <th>
                @Html.DisplayNameFor(model => model.ParentProductCategoryId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Rowguid)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ModifiedDate)
            </th>
            *@
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProductCategoryId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            @*
            <td>
                @Html.DisplayFor(modelItem => item.ParentProductCategoryId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rowguid)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ModifiedDate)
            </td>
           *@
            <td>
                @*@Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |*@
                @Html.ActionLink("Go ", "Product", new { id = item.ProductCategoryId }, new { @class = "btn btn-info" })
                @*@Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })*@
            </td>
        </tr>
}
    </tbody>
</table>
